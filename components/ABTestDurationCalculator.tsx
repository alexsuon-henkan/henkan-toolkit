// And update the Alert usage in the JSX:
{
  /* {results && (
  <div className="mt-6">
    {testObjective === "unknown" ? (
      <WeeklyMDETable results={results} />
    ) : (
      <Alert>
        <AlertDescription>
          <div className="space-y-2">
            <p>
              <strong>Test Duration:</strong> {results.durationInDays} days
            </p>
            <p>
              <strong>Sample Size per Variation:</strong> {results.sampleSizePerVariation.toLocaleString()} visitors
            </p>
            <p>
              <strong>Total Sample Size:</strong> {results.totalSampleSize.toLocaleString()} visitors
            </p>
          </div>
        </AlertDescription>
      </Alert>
    )}
  </div>
)} */
}
